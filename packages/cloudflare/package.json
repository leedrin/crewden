{
  "name": "@crewden/cloudflare",
  "version": "2.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @crewden/hub-core build && wrangler dev",
    "deploy": "pnpm run deploy:prod",
    "deploy:prod": "pnpm --filter @crewden/hub-core build && wrangler deploy --keep-vars --var CREWDEN_VERSION:${CREWDEN_VERSION:-2.0.0} --var CREWDEN_COMMIT_SHA:${CREWDEN_COMMIT_SHA:-} --var CREWDEN_BUILD_ID:${CREWDEN_BUILD_ID:-}",
    "deploy:test": "pnpm --filter @crewden/hub-core build && wrangler deploy --config wrangler.test.jsonc --keep-vars --var CREWDEN_ENV:test --var CREWDEN_VERSION:${CREWDEN_VERSION:-2.0.0-test} --var CREWDEN_COMMIT_SHA:${CREWDEN_COMMIT_SHA:-} --var CREWDEN_BUILD_ID:${CREWDEN_BUILD_ID:-}",
    "typecheck": "pnpm --filter @crewden/hub-core build && tsc --noEmit",
    "test": "pnpm --filter @crewden/hub-core build && vitest run"
  },
  "dependencies": {
    "@crewden/hub-core": "workspace:*",
    "@crewden/shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.41",
    "@cloudflare/workers-types": "^4.20260423.0",
    "@vitest/runner": "2.1",
    "@vitest/snapshot": "2.1",
    "typescript": "^5.4.5",
    "vitest": "^2.1.9",
    "wrangler": "^4.45.0"
  }
}
