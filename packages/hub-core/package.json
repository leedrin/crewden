{
  "name": "@crewden/hub-core",
  "version": "2.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "pnpm --filter @crewden/shared build && tsc",
    "typecheck": "pnpm --filter @crewden/shared build && tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@crewden/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
