{
  "name": "@crewden/server",
  "version": "2.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "pnpm --filter @crewden/hub-core build && pnpm --filter @crewden/paseo-client build && tsc",
    "typecheck": "pnpm --filter @crewden/hub-core build && pnpm --filter @crewden/paseo-client build && tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@fastify/websocket": "^8.3.1",
    "@libsql/client": "^0.17.3",
    "@crewden/hub-core": "workspace:*",
    "@crewden/paseo-client": "workspace:*",
    "@crewden/shared": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "fastify": "^4.27.0",
    "nanoid": "^5.0.7",
    "ws": "^8.17.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "@types/ws": "^8.5.10",
    "drizzle-kit": "^0.31.10",
    "tsx": "^4.11.0",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
