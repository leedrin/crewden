# v2.10 — Self-Evolution

> **给 Claude Code / Codex 的执行指令：** 实现本文档描述的 v2.10。按 TDD 工作：先写失败测试，再实现，再跑测试确认通过。完成后运行 `pnpm verify`。

## 背景

v2.8 建立了 Knowledge Layer，v2.9 建立了 Trace 系统（含每一次 Task 的 Run history 数据）。v2.10 利用这些数据实现轻量自进化——从成功的 Task 中提取可复用的 Skill，从失败的 Task 中提取教训（依赖 v2.9 Trace 中的 Run 记录和 outcome），并维护知识库的卫生。

## 目标

- Skill capture：Task done（Review approved）→ 提取可复用模式 → Skill 草稿
- Improvement note：Task 失败后重试成功 → "下次遇到 X 先做 Y" → learning
- Knowledge hygiene：定期扫描 stale / conflict 并通知
- 不引入独立 Curator Agent——嵌入到 Task 闭环中

## 设计参考

> 参考 [MultiCa 任务系统可行性调研](./multica-task-board-feasibility.md)，v2.10 可从以下资产中提取设计经验：
来源文件位于/Users/lijun/Documents/multica目录下
| 参考来源 | 来源文件 | 提取内容 |
|---------|---------|---------|
| **MultiCa** | `skill` + `skill_file` 表结构 | Skill 作为可复用知识单元的存储模型——Crewden 扩展为从 Task 自动沉淀 |
| **MultiCa** | Skill 手动创建 / URL 导入流程 | Skill 来源的触发时机——Crewden 扩展为 Task done → 自动生成 draft |

> v2.10 的核心逻辑（Task done → Skill capture + Improvement note + Knowledge hygiene）为 Crewden 原创设计。MultiCa 的 Skill 是静态手动创建，Crewden 扩展为动态自动沉淀 + 状态机（draft → verified → deprecated）。

## project 影响（v2.2.1）

> 详见 [v2.2.1 — Project 多项目隔离](./v2.2.1-project-isolation.md)

| 影响点 | 说明 |
|-------|------|
| Skill capture | Task done 后提取的 Skill 归属当前 project（`scope=project`，`skill_status=draft`） |
| Improvement note | 失败重试成功的 improvement note 归属当前 project（`scope=project`，`kind=learning`） |
| Knowledge hygiene | 卫生扫描按 project 隔离：stale / conflict 检测不跨 project |
| Skill 升级 | `usage_count` 统计按 project 汇总（同 project 内使用才算引用） |
| applicable_roles | Skill 的适用角色限定当前 project 的 Agent role 列表 |

---

## 数据模型

```sql
-- Skill 是 Knowledge Entry 的特殊子集
-- kind = 'artifact' or 'runbook', 额外字段：
ALTER TABLE knowledge_entries ADD COLUMN skill_status TEXT;
  -- draft | verified | deprecated（独立于 knowledge status 的 active/stale/archived）

ALTER TABLE knowledge_entries ADD COLUMN applicable_roles TEXT;        -- JSON array
ALTER TABLE knowledge_entries ADD COLUMN applicable_capabilities TEXT; -- JSON array

ALTER TABLE knowledge_entries ADD COLUMN usage_count INTEGER NOT NULL DEFAULT 0;
ALTER TABLE knowledge_entries ADD COLUMN last_used_at INTEGER;
```

---

## 实现任务

### Task 1：Task → Skill Capture

Task done（Review approved）+ type = 'feature'/'bug'/'chore' + latestRun.summary 有价值时：

```typescript
async function captureSkillFromTask(task: TaskV2): Promise<KnowledgeEntry> {
  // 1. 提取执行模式
  const steps = task.latestRun?.metadata?.steps || [];
  const tools = task.latestRun?.toolCalls?.map(t => t.toolName) || [];
  const files = task.latestRun?.metadata?.changedFiles || [];

  // 2. 生成 Skill 草稿
  return createKnowledgeEntry({
    kind: 'artifact',
    scope: 'workspace',
    title: `[Draft] Pattern: ${task.title.split(' ').slice(0, 3).join(' ')}`,
    summary: task.latestRun.summary,
    body: `## Steps\n${steps.map(s => `- ${s}`).join('\n')}\n\n## Tools Used\n${tools.join(', ')}\n\n## Files\n${files.join(', ')}`,
    tags: [task.type, ...(task.owner?.role ? [task.owner.role] : [])],
    source_refs: [task.id, task.prUrl].filter(Boolean),
    skill_status: 'draft',
    applicable_roles: [task.owner?.role].filter(Boolean),
  });
}
```

只生成 draft，需要 Human 确认后才 verified。

### Task 2：Task → Improvement Note

Task 失败后重试成功时（latestRun.outcome='completed' + 之前有 failed Run）：

```typescript
async function captureImprovementNote(task: TaskV2): Promise<KnowledgeEntry> {
  const failedRun = task.runs.find(r => r.outcome === 'failed');
  const successfulRun = task.runs.find(r => r.outcome === 'completed');

  return createKnowledgeEntry({
    kind: 'learning',
    scope: 'workspace',
    title: `[Auto] Improvement: ${task.title}`,
    summary: `Task required ${task.runs.length} attempts to complete.`,
    body: `## Failure\n${failedRun?.error || 'Unknown error'}\n\n## Solution\n${successfulRun?.summary || 'Unknown solution'}`,
    tags: ['improvement', task.type],
    source_refs: [task.id],
    status: 'active',
  });
}
```

### Task 3：Knowledge Hygiene Scanner

定期任务（每天）：

```typescript
async function runKnowledgeHygiene(): Promise<void> {
  // 1. 扫描 30 天未被引用 → 标记 stale
  // 2. 扫描内容相似（title 相同）但冲突 → 标记 conflict
  // 3. 扫描 superseded 的 Decision → 标记对应 Knowledge 为 archived
}
```

### Task 4：Skill 升级

```typescript
// draft → verified（需要 Human 手动操作或 usage_count > 阈值）
async function promoteSkill(skillId: string): Promise<void> {
  // usage_count > 5 且无 conflict 标记 → 自动建议 verified
  // 在 Agent inbox 中推一条通知
}
```

### Task 5：Agent-facing CLI

```bash
crewden knowledge hygiene                # 运行知识卫生检查
crewden skill promote <knowledgeId>       # 提升 skill 状态
```

---

## 验收条件

- [ ] Task done 自动生成 Skill draft（kind=artifact, skill_status=draft）
- [ ] Task 失败后重试成功生成 improvement note（kind=learning）
- [ ] Knowledge hygiene 扫描 stale 和 conflict
- [ ] Draft Skill 在 usage_count > 5 时建议升级
- [ ] 不引入独立 Curator Agent——内嵌到 Task 闭环
- [ ] `pnpm verify` 全绿

## 手动验证

1. 创建 Task → Agent 执行成功 → 确认生成 Skill draft
2. 另一个 Task 执行相同模式 → 确认该 Skill usage_count 增加
3. 创建 Task → 第一次失败 → 第二次成功 → 确认生成 improvement note
4. 运行 knowledge hygiene → 确认 30 天未使用的 knowledge 标记 stale

## 完成后执行

```bash
pnpm verify
git add -A
git commit -m "feat(v2): add self-evolution and skill capture"
git push
```
